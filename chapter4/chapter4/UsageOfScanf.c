#include <stdio.h>
#include <inttypes.h> // intmax_t

int main(){

    // int i;
    // float f;
    // char str[30];

    // scanf("%d %f %s",&i ,&f,str);
    // printf("%d %f %s\n",i,f,str);

    //character - with ascill

    // char c;
    // scanf("%c",&c); // try blank
    // printf("%i",c); // blank is 32

    //Unsigned as signed 
    // unsigned i;
    // scanf("%i",&i);         //try negative number but not good way;
    // printf("%i\n",i);

    //unsigned as unsigned ;

    // unsigned int i2;
    // scanf("%u" , &i2);
    // printf("%u", i2);

    //floating point numbers;

    // double d = 0.0;
    // scanf("%lf" , &d);
    // printf("%lf\n", d);

    //width

    // char str[30];
    // scanf("%5s",str); // width
    // printf("%s\n",str);

    // h modifier
    // char i;
    // scanf("%hhd",&i); //try large number;
    // printf("%i\n" , i);

    // integer with characters

    int i;
    scanf("%i",&i);
    printf("%i\n" , i);
    
    
















    return 0;

}